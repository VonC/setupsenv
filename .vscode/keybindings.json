// Place your key bindings in this file to override the defaults
[
    {
        "key": "ctrl+shift+/",
        "command": "replacerules.runRuleset",
        "when": "editorTextFocus && !editorReadonly",
        "args": {
            "rulesetName": "Update xxx messages"
        }
    }
]