@echo off
rem Team-wide environment variables, sourced by every senv session.
rem Typical content, behind a corporate proxy (see the px tool):
rem   set "HTTP_PROXY=http://127.0.0.1:3128"
rem   set "HTTPS_PROXY=http://127.0.0.1:3128"
rem   set "NO_PROXY=localhost,127.0.0.1,.example.corp"
