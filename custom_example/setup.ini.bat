@echo off
REM local  folder (on a local drive)
REM where programs archives are uncompressed and programs installed. 
set PRGS=
REM local or (better) remote folder (on a network drive)
REM where config files are keps. 
set HOME=
REM local folder (on a local drive)
REM where Git repositories and developments are done. 
set PROG=
